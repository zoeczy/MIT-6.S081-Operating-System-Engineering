1. a0-a7 is the func arguments,a0 save the return value. a2:13
2. inline f(8)+1 = 12
3. auipc ra,0x0 执行完后，ra里的内容就是这条指令的地址，也就是0x30.jalr 1530(ra) # 628 <printf>

所以跳转的地址是0x30+十进制的1536，也就是0x630. 
4. ra = pc+4 = 0x38
5. He110 World   ％x 表示以十六进制数形式输出整数，57616 的16进制表示就是 e110，与大小端序无关。
%s 是输出字符串，以整数 i 所在的开始地址，按照字符的格式读取字符，直到读取到 ‘\0’ 为止。当是小端序表示的时候，内存中存放的数是：72 6c 64 00，刚好对应rld。当是大端序的时候，则反过来了，因此需要将 i 以16进制数的方式逆转一下。
6. printf 接收到了两个参数，但实际需要三个参数，最后一个参数是放在 a2 寄存器中，由于没有输入第三个参数，因此 a2 寄存器中目前有啥就输出啥。 
